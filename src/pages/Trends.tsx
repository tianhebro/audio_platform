import { InsightCard } from '../components/InsightCard';
import { SectionHeader } from '../components/SectionHeader';
import { directionMap, insights } from '../data/insights';
export function Trends() { return <section className="mx-auto max-w-7xl px-4 py-14 md:px-6 lg:px-8"><SectionHeader eyebrow="Insights" title="趋势观察与方向图谱" description="用专题调研、月度观察和行业趋势卡片追踪声音 AI 从模型到产品的演进。" /><div className="mt-8 rounded-[2rem] bg-aurora p-8 text-white"><h2 className="text-2xl font-bold">方向图谱</h2><div className="mt-6 grid gap-3 md:grid-cols-3 lg:grid-cols-6">{directionMap.map((item, index) => <div key={item} className="rounded-2xl border border-white/15 bg-white/10 p-4 text-center"><span className="mx-auto mb-3 flex h-9 w-9 items-center justify-center rounded-full bg-white text-sm font-bold text-blue-700">{index + 1}</span><p className="text-sm font-semibold">{item}</p></div>)}</div></div><div className="mt-8 grid gap-5 md:grid-cols-3">{insights.map((insight) => <InsightCard key={insight.title} insight={insight} />)}</div></section>; }
